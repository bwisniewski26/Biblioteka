@page "/refresh"
@page "/refresh/{Text}"
@inject NavigationManager Navigator

@code {
    [Parameter]
    public string Text { get; set; } = "";

    protected override void OnInitialized()
    {
        if (Text == "accountmanager")
        {
            Navigator.NavigateTo("/accountmanager/");
            return;
        }
        Navigator.NavigateTo("/account/");
    }

}